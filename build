#!/usr/bin/env bash
# Convenience wrapper so `./build` works from Git Bash; runs the real
# PowerShell build script (build.ps1 -> tools/build_manager.ps1).
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")"
exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File build.ps1 "$@"
